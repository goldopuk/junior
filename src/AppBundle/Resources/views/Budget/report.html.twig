{% extends 'base.html.twig' %}

{% block body %}
    <script>
        var listSumByMonth = {{ listSumByMonth|json_encode|raw }};
        var listIncomeByMonth = {{ listIncomeByMonth|json_encode|raw }};
    </script>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}


    <div id="wrapper">
        <div id="container">
            <table>
                {%  for sum in listSumByYear  %}
                <tr>
                    <td>{{ sum.date }}</td>
                    <td>{{ sum.amount }}</td>
                </tr>
                {% endfor %}
            </table>
            Income
            <table>
                {%  for sum in listIncomeByYear  %}
                    <tr>
                        <td>{{ sum.date }}</td>
                        <td>{{ sum.amount }}</td>
                    </tr>
                {% endfor %}
            </table>

        </div>
    </div>

{% endblock %}

{% block javascripts %}
    <script src="{{ asset('js/report.js') }}"></script>
{% endblock %}